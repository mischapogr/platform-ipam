# Claude Code project instructions

Read [AGENTS.md](AGENTS.md) for the shared project boundaries, task routing, and
validation rules. For planned work, use the one package contract in
[docs/WORK_PLAN.md](docs/WORK_PLAN.md). The plan is shared with Codex; select
Claude Code's model from its column or use `scripts/ai/run-work-plan --tool claude`.
